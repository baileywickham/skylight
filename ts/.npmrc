; A real, self-contained node_modules (npm's layout), not pnpm's symlink farm
; into the global store: `skylight-run` stages the client into
; ~/Library/Application Support/skylight/ts-<version> and installs there, and a
; tree of symlinks would break the moment the store is pruned or moved.
node-linker=hoisted
